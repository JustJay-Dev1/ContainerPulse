apiVersion: v1
kind: ConfigMap

metadata:
  name: containerpulse-config
  namespace: containerpulse

data:
  METRICS_INTERVAL: "5"
  DATABASE_HOST: "postgres"
  DATABASE_PORT: "5432"
  DATABASE_NAME: "monitoring"